default: &default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>

development:
  <<: *default
  database: manmaru_shiba_development

test:
  <<: *default
  database: manmaru_shiba_test

production:
  <<: *default
  url: <%= ENV["DATABASE_URL"] %>
